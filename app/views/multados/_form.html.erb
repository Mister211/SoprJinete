<%= form_with(model: multado, local: true) do |form| %>
  <% if multado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multado.errors.count, "error") %> prohibited this multado from being saved:</h2>

      <ul>
        <% multado.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.text_field :fecha %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="field">
    <%= form.label :Prestamo_id %>
    <%= form.text_field :Prestamo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
